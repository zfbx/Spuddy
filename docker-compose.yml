services:
  nadeko:
    build:
      context: .
    container_name: nadeko
    restart: unless-stopped
    environment:
      TZ: America/New_York
    volumes:
      - nadekodata:/app/data
networks: {}
volumes:
  nadekodata: